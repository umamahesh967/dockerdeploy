pipeline {
    agent { dockerfile true }
    stages {
        stage('Build') {
            steps {
               echo 'Im simple'
            }
        }
   
    stage('deploy') {
            steps {
                sh 'docker-compose -f docker-compose.yml up'
            }
        }

    }
}
